services:
  wrapper-manager:
    build: .
    container_name: wrapper-manager
    privileged: true
    command: [
      "--host", "0.0.0.0",
      "--port", "8080",
      "--mirror", "false"
    ]
    ports:
      - "8080:8080"
    volumes:
      - data:/root/data
      
volumes:
  data:
